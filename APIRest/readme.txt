tiene que estar cargado node
para comenzar
desde la teminal y en la carpeta APIRest
json-server -w db.json
para terminar ctrl + C
para cambiar de port agregar -p y el numero de puerto(por default 3000)
ej.: json-server -w -p 5000 db.json

Con insomnia puedo probar todas las acciones de una API Rest
